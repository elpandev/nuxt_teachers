import { BaseRule, required, string, min, array, integer, float } from "./rule";
import { Validate } from "./validate";
import { Validator } from "./validator";
import type { IErrors } from "./validator";

export {
  Validator,
  Validate,
  BaseRule,
  required,
  string,
  min,
  array,
  integer,
  float,

  type IErrors,
}
